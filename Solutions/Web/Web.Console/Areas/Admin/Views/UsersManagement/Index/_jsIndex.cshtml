<script type="text/javascript">

    $(document).ready(function () {

        var roleId = 0;

        var levelId = 0;

        $('#slcDomainSearch')
            .append('<option value="" disabled>---</option>');

        for (var i = 0; i < domainsSettingsList.length; i++) {
            var item = domainsSettingsList[i];
            $('#slcDomainSearch')
                .append($("<option></option>")
                    .attr("value", item.Value)
                    .text(item.DisplayText));
        }

        $('#slcDomainSearch').chosen({
            placeholder_text_single: ' ',
            placeholder_text_multiple: ' ',
            search_contains: true,
            max_shown_results: numberOfChosenItemsShow,
            rtl: rtl
        });

        $('#slcDomainAndRole').chosen({
            placeholder_text_single: ' ',
            placeholder_text_multiple: ' ',
            search_contains: true,
            max_shown_results: numberOfChosenItemsShow,
            rtl: rtl
        });

        $('#slcRoleIdsSearch')
            .append('<option value="" disabled>---</option>');

        for (var i = 0; i < rolesList.length; i++) {
            var item = rolesList[i];
            $('#slcRoleIdsSearch')
                .append($("<option></option>")
                    .attr("value", item.RoleId)
                    .attr("data-DomainSettingId", item.DomainSettingId)
                    .text(((item.RoleName == 'Admins') ? 'داخلی' : 'خارجی')));
        }

        $('#slcRoleIdsSearch').chosen({
            placeholder_text_single: ' ',
            placeholder_text_multiple: ' ',
            search_contains: true,
            max_shown_results: numberOfChosenItemsShow,
            rtl: rtl
        });

        $('#slcLevelIdsSearch')
            .append('<option value="" disabled>---</option>');

        $('#slcLevelIdsSearch').chosen({
            placeholder_text_single: ' ',
            placeholder_text_multiple: ' ',
            search_contains: true,
            max_shown_results: numberOfChosenItemsShow,
            rtl: rtl
            //placeholder_text_multiple: placeholder_text_multiple
        });

        $('#slcParentIdSearch').find('option')
            .remove()
            .end()
            .append('<option value="" disabled>---</option>');

        $('#slcParentIdSearch').chosen({
            placeholder_text_single: ' ',
            placeholder_text_multiple: ' ',
            search_contains: true,
            max_shown_results: numberOfChosenItemsShow,
            rtl: rtl
        });

        $('#slcRoleIdsSearch').change(function () {
            debugger;

            var _this = $(this);
            var roleIds = _this.val();

            $('#slcLevelIdsSearch').find('option')
                .remove()
                .end()
                .append('<option value="" disabled>---</option>');

            $('#slcLevelIdsSearch').trigger("chosen:updated");

            //$('#slcParentIdSearch').find('option')
            //    .remove()
            //    .end()
            //    .append('<option value="" disabled>---</option>');

            //$('#slcParentIdSearch').trigger("chosen:updated");

            var domainSettingId = $('#slcDomainSearch').val();
            //debugger;

            //$.ajax({
            //    cache: false,
            //    type: "Post",
            //    url: "/Admin/UsersManagement/GetUsersList",
            //    data: /*JSON.stringify(*/{
            //        'DomainSettingId': domainSettingId,
            //        'RoleId': roleId,
            //    }/*)*/,
            //    //contentType: 'application/json',
            //    //dataType: "json",
            //    success: function (result) {
            //        if (result.Result == "OK") {
            //            debugger;
            //            var usersList = result.Records;

            //            for (var i = 0; i < usersList.length; i++) {
            //                var item = usersList[i];
            //                $('#slcParentIdSearch')
            //                    .append($("<option></option>")
            //                        .attr("value", item.UserId)
            //                        .text(item.UserName +
            //                        ((item.Name == null) ? '' : ' - ' + item.Name) +
            //                        ((item.Family == null) ? '' : ' - ' + item.Family)));
            //            }
            //            $('#slcParentIdSearch').chosen({
            //                placeholder_text_single: ' ',
            //                placeholder_text_multiple: ' ',
            //                search_contains: true,
            //                max_shown_results: numberOfChosenItemsShow,
            //                rtl: rtl
            //            });
            //            $('#slcParentIdSearch').trigger("chosen:updated");
            //        }
            //        else
            //            if (result.Result == "ERROR") {
            //                $('#my-warning-modal').find('.modal-title').html('خطا');
            //                $('#myModal').find('#error').html(result.Message);
            //                $('#myModal').modal("show");
            //            }
            //    },
            //    error: function (xhr, ajaxOptions, thrownError) {
            //        $('#my-warning-modal').find('.modal-title').html('خطا');
            //        $('#my-warning-modal').find('#error').html('مقادیر را وارد کنید');
            //        $('#my-warning-modal').modal("show");
            //    }
            //});

            debugger;

            $.ajax({
                cache: false,
                type: "Post",
                url: "/Admin/UsersManagement/GetLevelsList",
                data: /*JSON.stringify(*/{
                    'DomainSettingId': domainSettingId,
                    'RoleIds': roleIds,
                }/*)*/,
                //contentType: 'application/json',
                //dataType: "json",
                success: function (result) {
                    if (result.Result == "OK") {
                        debugger;
                        var levelsList = result.Records;

                        for (var i = 0; i < levelsList.length; i++) {
                            var item = levelsList[i];
                            $('#slcLevelIdsSearch')
                                .append($("<option></option>")
                                    .attr("value", item.LevelId)
                                    .text(item.LevelName));
                        }
                        $('#slcLevelIdsSearch').chosen({
                            placeholder_text_single: ' ',
                            placeholder_text_multiple: ' ',
                            search_contains: true,
                            max_shown_results: numberOfChosenItemsShow,
                            rtl: rtl
                        });

                        if (levelId == 0)
                            $('#slcLevelIdsSearch').trigger("chosen:updated");
                        else
                            $('#slcLevelIdsSearch').val(levelId).trigger("chosen:updated");
                        ShowCheckBoxBeforeChosenForSpecificElement($('#slcLevelIdsSearch'));

                    }
                    else
                        if (result.Result == "ERROR") {
                            $('#my-warning-modal').find('.modal-title').html('خطا');
                            $('#myModal').find('#error').html(result.Message);
                            $('#myModal').modal("show");
                        }
                },
                error: function (xhr, ajaxOptions, thrownError) {
                    $('#my-warning-modal').find('.modal-title').html('خطا');
                    $('#my-warning-modal').find('#error').html('مقادیر را وارد کنید');
                    $('#my-warning-modal').modal("show");
                }
            });

        });

        $('#slcDomainSearch').change(function () {
            debugger;

            var _this = $(this);
            var domainSettingId = _this.val();
            var roleIds = $('#slcRoleIdsSearch').val();

            $('#slcLevelIdsSearch').find('option')
                .remove()
                .end()
                .append('<option value="" disabled>---</option>');

            $('#slcLevelIdsSearch').trigger("chosen:updated");

            $('#slcParentIdSearch').find('option')
                .remove()
                .end()
                .append('<option value="" disabled>---</option>');

            $('#slcParentIdSearch').trigger("chosen:updated");

            $.ajax({
                cache: false,
                type: "Post",
                 url: "/Admin/UsersManagement/GetUsersList",
                
                data: /*JSON.stringify(*/{
                    'DomainSettingId': domainSettingId,
                    //'RoleId': roleId,
                }/*)*/,
                //contentType: 'application/json',
                //dataType: "json",
                success: function (result) {
                    if (result.Result == "OK") {
                        //debugger;
                        var usersList = result.Records;

                        $('#slcParentIdSearch').find('option')
                            .remove()
                            .end()
                            .append('<option value="" disabled>---</option>');

                        $('#slcParentIdSearch').trigger("chosen:updated");

                        for (var i = 0; i < usersList.length; i++) {
                            var item = usersList[i];
                            $('#slcParentIdSearch')
                                .append($("<option></option>")
                                    .attr("value", item.UserId)
                                    .text(item.UserName +
                                        ((item.Name == null) ? '' : ' - ' + item.Name) +
                                        ((item.Family == null) ? '' : ' - ' + item.Family)));
                        }
                        $('#slcParentIdSearch').chosen({
                            placeholder_text_single: ' ',
                            placeholder_text_multiple: ' ',
                            search_contains: true,
                            max_shown_results: numberOfChosenItemsShow,
                            rtl: rtl
                        });
                        $('#slcParentIdSearch').trigger("chosen:updated");

                        ShowCheckBoxBeforeChosenForSpecificElement($('#slcParentIdSearch'));

                        //$('#slcLevelIdSearch').find('option')
                        //    .remove()
                        //    .end()
                        //    .append('<option value="" disabled>---</option>');

                        //$('#slcLevelIdSearch').chosen({
                        //    placeholder_text_single: ' ',
                        //    placeholder_text_multiple: ' ',
                        //    search_contains: true,
                        //    max_shown_results: numberOfChosenItemsShow,
                        //    rtl: rtl
                        //});

                        //$('#slcLevelIdSearch').trigger("chosen:updated");

                        //var slcParentIdSearch = $('#slcParentIdSearch').val();

                        //if (roleId != '') {
                        //    for (var i = 0; i < levelsList.length; i++) {
                        //        var item = levelsList[i];
                        //        if ((item.RoleId == roleId) &&
                        //            ((slcParentIdSearch != '') ?
                        //                (item.UserIdCreator == slcParentIdSearch) :
                        //                (item.UserIdCreator == userId))) {
                        //            //if (item.RoleId == roleId) {
                        //            $('#slcLevelIdSearch')
                        //                .append($("<option></option>")
                        //                    .attr("value", item.RoleId)
                        //                    .attr("data-RoleId", item.roleId)
                        //                    .text(item.LevelName));
                        //        }
                        //    }
                        //    $('#slcLevelIdSearch').chosen({
                        //        placeholder_text_single: ' ',
                        //        placeholder_text_multiple: ' ',
                        //        search_contains: true,
                        //        max_shown_results: numberOfChosenItemsShow,
                        //        rtl: rtl
                        //    });
                        //    $('#slcLevelIdSearch').trigger("chosen:updated");
                        //}
                    }
                    else
                        if (result.Result == "ERROR") {
                            $('#my-warning-modal').find('.modal-title').html('خطا');
                            $('#myModal').find('#error').html(result.Message);
                            $('#myModal').modal("show");
                        }
                },
                error: function (xhr, ajaxOptions, thrownError) {
                    $('#my-warning-modal').find('.modal-title').html('خطا');
                    $('#my-warning-modal').find('#error').html('مقادیر را وارد کنید');
                    $('#my-warning-modal').modal("show");
                }
            });

            debugger;

            $.ajax({
                cache: false,
                type: "Post",
                url: "/Admin/UsersManagement/GetLevelsList",
                data: /*JSON.stringify(*/{
                    'DomainSettingId': domainSettingId,
                    'RoleIds': roleIds,
                }/*)*/,
                //contentType: 'application/json',
                //dataType: "json",
                success: function (result) {
                    if (result.Result == "OK") {
                        debugger;
                        var levelsList = result.Records;

                        for (var i = 0; i < levelsList.length; i++) {
                            var item = levelsList[i];
                            $('#slcLevelIdsSearch')
                                .append($("<option></option>")
                                    .attr("value", item.LevelId)
                                    .text(item.LevelName));
                        }
                        $('#slcLevelIdsSearch').chosen({
                            placeholder_text_single: ' ',
                            placeholder_text_multiple: ' ',
                            search_contains: true,
                            max_shown_results: numberOfChosenItemsShow,
                            rtl: rtl
                        });

                        if (levelId == 0)
                            $('#slcLevelIdsSearch').trigger("chosen:updated");
                        else
                            $('#slcLevelIdsSearch').val(levelId).trigger("chosen:updated");
                        ShowCheckBoxBeforeChosenForSpecificElement($('#slcLevelIdsSearch'));

                    }
                    else
                        if (result.Result == "ERROR") {
                            $('#my-warning-modal').find('.modal-title').html('خطا');
                            $('#myModal').find('#error').html(result.Message);
                            $('#myModal').modal("show");
                        }
                },
                error: function (xhr, ajaxOptions, thrownError) {
                    $('#my-warning-modal').find('.modal-title').html('خطا');
                    $('#my-warning-modal').find('#error').html('مقادیر را وارد کنید');
                    $('#my-warning-modal').modal("show");

                }
            });


        });

        $('#slcParentIdSearch').change(function () {
            debugger;
        });

        $('.my-select-all').click(function () {
            $('#my_select option').prop('selected', true); // Selects all options
        });

        $('#listContainer').jtable({
            //messages: persianMessages,
            title: 'لیست', toolbarType: 'both',
            paging: true,
            pageSize: 10,
            selecting: true,
            multiselect: (($.cookie("MultiSelect") != "true") ? false : true),
            selectingCheckboxes: ($.cookie("MultiSelect") != "true") ? false : true,
            //selecting: true,
            sorting: true,
            //defaultSorting: 'Name ASC',
            actions: {
                listAction: '/Admin/UsersManagement/GetListOfUsers',
                //createAction: '/Admin/UsersManagement/AddToLevels',
                //updateAction: '/Admin/UsersManagement/UpdateLevel',
                deleteAction: '/Admin/UsersManagement/CompleteDeleteUser'
            },
            toolbar: {
                /*hoverAnimation: true,
                hoverAnimationDuration: 60,
                hoverAnimationEasing: 'swing',*/
                items: [
                    {
                        icon: '/images/add.png',
                        // text: 'تعریف  کاربر',
                        text: 'افزودن',
                        click: function () {
                            window.location.href = '/Admin/UsersManagement/CreateUser';
                        }
                    },
                    {
                        icon: '',
                        text: 'راهنمایی',
                        click: function () {
                            debugger;
                            $('#my-warning-modal').find('.modal-title').html('راهنمایی');
                            $('#my-warning-modal').find('#error').html('<div class="row">' +
                                '<div class="col col-md-12 col-xs-12 col-sm-12 col-lg-12">' +
                                '<i class="fa fa-2x fa-square-o" style=""></i>' +
                                'فعال/حذف نشده' +
                                '<br />' +
                                '<i class="fa fa-2x fa-square" style="color:#cc9999"></i>' +
                                'حذف شده/فعال' +
                                '<br />' +
                                '<i class="fa fa-2x fa-square" style="color:#ff3333"></i>' +
                                'حدف شده/غیر فعال' +
                                '<br />' +
                                '<i class="fa fa-2x fa-square" style="color:#f0ad4e"></i>' +
                                'حذف نشده/غیر فعال' +
                                '</div>' +
                                '</div>');
                            $('#my-warning-modal').modal("show");
                            $('#my-warning-modal').removeClass("fade");
                        }
                    },
                    {
                        icon: '',
                        text: 'تغییر نمایش',
                        click: function () {
                            $.ajax({
                                cache: false,
                                type: "Post",
                                url: '/Admin/AdminPanel/ChangeViewMode',
                                //data: /*JSON.stringify(*/{
                                //    'lang': lang
                                //}/*)*/,
                                //contentType: 'application/json',
                                //dataType: "json",
                                success: function (result) {
                                    if (result.Result == "OK") {
                                        window.setTimeout(function () {
                                            location.reload(true);
                                        }, 500);
                                    }
                                    else
                                        if (result.Result == "ERROR") {
                                            alert('error');
                                        }
                                },
                                error: function (xhr, ajaxOptions, thrownError) {
                                }
                            });
                        }
                    }]
            },
            fields: {
                UserId: {
                    key: true,
                    list: false
                },
                Picture: {
                    title: 'تصویر',
                    display: function (data) {
                        //debugger;
                        if (data.record.Picture != null) {
                            if (data.record.Picture != '') {
                                return '<div style="position: relative; display: inline-block;" class="hoverable">' +
                                    '<a class="fancybox userThumbImage" href="/Files/UserFiles/' + data.record.DomainName + '/' +
                                    data.record.UserId + '/' + data.record.Picture + '" data-fancybox-group="gallery" ' +
                                    'title=""><img src="/Files/UserFiles/' + data.record.DomainName + '/' +
                                    data.record.UserId + '/' + data.record.Picture + '" alt="" ' +
                                    'data-caption="' + data.record.UserName + '" data-url="/Admin/UserManagement/GetUserDetails/' +
                                    data.record.UserId + '" /></a>' + '<i class="fa fa-2x fa-trash-o hide pointer removeClick" ' +
                                    'data-href="/Admin/UsersManagement/RemoveUserPicture" ' +
                                    'data-Id="' + data.record.UserId + '"></i></div>';
                            }
                        }
                        return '<div class="align-center"><i class="fa fa-3x fa-user">&nbsp;</i></div>';
                    }
                },
                UserName: {
                    title: 'نام&nbsp;کاربری',
                    display: function (data) {
                        return '<div class="isActivated" style="display:none;">' + data.record.IsActivated + '</div>' +
                            '<div class="isDeleted" style="display:none;">' + data.record.IsDeleted + '</div>' +
                            data.record.UserName;
                    }
                },
                Email: {
                    title: 'ایمیل',
                },
                Name: {
                    title: 'نام',
                },
                Family: {
                    title: 'نام&nbsp;خانوادگی',
                },
                NameFamilyOfUserCreator: {
                    //title: 'کاربر&nbsp;تعریف&nbsp;کننده',
                    title: 'والد',
                },
                DomainSettingId: {
                    title: 'دامنه',
                    sorting: false,
                    options: domainsSettingsList
                },
                RoleId: {
                    title: 'نقش',
                    //options: rolesList,
                    sorting: false,
                    display: function (data) {
                        debugger;
                        if (data.record.RoleName)
                            return ((data.record.RoleName == 'Admins') ? 'داخلی' : 'خارجی');
                        else
                            if (data.record.RoleNames)
                                return data.record.RoleNames.replace(/Admins/g, 'داخلی').replace(/Users/g, 'خارجی');
                            else
                                if (data.record.StrRoleNames)
                                    return data.record.StrRoleNames.replace(/Admins/g, 'داخلی').replace(/Users/g, 'خارجی');
                    }
                    //inputClass: 'validate[required]'
                },
                LevelName: {
                    title: 'دسترسی',
                    display: function (data) {
                        ////debugger;
                        if (data.record.LevelName)
                            return data.record.LevelName;
                        else
                            if (data.record.LevelNames)
                                return data.record.LevelNames;
                            else
                                if (data.record.StrLevelNames)
                                    return data.record.StrLevelNames;
                    }
                },
                Phone: {
                    title: 'تلفن',
                    sorting: false,
                },
                Mobile: {
                    title: 'همراه',
                    sorting: false,
                },
                Address: {
                    title: 'آدرس',
                    sorting: false,
                },
                CertificateId: {
                    title: 'شماره&nbsp;شناسنامه',
                    sorting: false,
                },
                NationalCode: {
                    title: 'کد&nbsp;ملی',
                    sorting: false,
                },
                BirthDateTime: {
                    title: 'تاریخ&nbsp;تولد',
                },
                RegisterDate: {
                    title: 'تاریخ&nbsp;ثبت&nbsp;نام',
                },
                HierarchyOfUsers: {
                    title: 'ساختار&nbsp;سلسله&nbsp;مراتبی',
                    sorting: false,
                    display: function (data) {
                        return '<div class="hierarchyOfUsers pointer" data-UserId="' + data.record.UserId + '"><i class="fa fa-sitemap"></i></div>';
                    }
                },
                HierarchyOfTreeViewUsers: {
                    title: 'ساختار&nbsp;درخنی',
                    sorting: false,
                    display: function (data) {
                        return '<div class="HierarchyOfTreeViewUsers pointer" data-UserId="' + data.record.UserId + '"><i class="fa fa-sitemap"></i></div>';
                    }
                },
                CertificateCardFile: {
                    title: 'شناسنامه',
                    sorting: false,
                    display: function (data) {
                        debugger;
                        if (data.record.CertificateCardFile != null) {
                            if (data.record.CertificateCardFile != '') {
                                return '<div style="position: relative; display: inline-block;" class="hoverable">' +
                                    '<a class="userThumbImage" href="/Files/UserFiles/' + data.record.DomainName + '/' +
                                    data.record.UserId + '/' + data.record.CertificateCardFile + '"><i class="fa fa-file" style="font-size:16px;">&nbsp;</i></a>' +
                                    '<i class="fa fa-2x fa-trash-o hide pointer removeClick" ' +
                                    'data-href="/Admin/UsersManagement/RemoveUserCertificate" ' +
                                    'data-Id="' + data.record.UserId + '"></i></div>';
                            }
                        }
                        return '----';
                    }
                },
                NationalCardFile: {
                    title: 'کارت&nbsp;ملی',
                    sorting: false,
                    display: function (data) {
                        //debugger;
                        if (data.record.NationalCardFile != null) {
                            if (data.record.NationalCardFile != '') {
                                return '<div style="position: relative; display: inline-block;" class="hoverable">' +
                                    '<a class="userThumbImage" href="/Files/UserFiles/' + data.record.DomainName + '/' +
                                    data.record.UserId + '/' + data.record.NationalCardFile + '"><i class="fa fa-file" style="font-size:16px;">&nbsp;</i></a>' +
                                    '<i class="fa fa-2x fa-trash-o hide pointer removeClick" ' +
                                    'data-href="/Admin/UsersManagement/RemoveUserNationalCard" ' +
                                    'data-Id="' + data.record.UserId + '"></i></div>';
                            }
                        }
                        return '----';
                    }
                },
                ResumeFile: {
                    title: 'رزومه',
                    sorting: false,
                    display: function (data) {
                        //debugger;
                        if (data.record.ResumeFile != null) {
                            if (data.record.ResumeFile != '') {
                                return '<div style="position: relative; display: inline-block;" class="hoverable">' +
                                    '<a class="userThumbImage" href="/Files/UserFiles/' + data.record.DomainName + '/' +
                                    data.record.UserId + '/' + data.record.ResumeFile + '"><i class="fa fa-file" style="font-size:16px;">&nbsp;</i></a>' +
                                    '<i class="fa fa-2x fa-trash-o hide pointer removeClick" ' +
                                    'data-href="/Admin/UsersManagement/RemoveUserResume" ' +
                                    'data-Id="' + data.record.UserId + '"></i></div>';
                            }
                        }
                        return '----';
                    }
                },
                ContractFile: {
                    title: 'قرارداد',
                    sorting: false,
                    display: function (data) {
                        //debugger;
                        if (data.record.ContractFile != null) {
                            if (data.record.ContractFile != '') {
                                return '<div style="position: relative; display: inline-block;" class="hoverable">' +
                                    '<a class="userThumbImage" href="/Files/UserFiles/' + data.record.DomainName + '/' +
                                    data.record.UserId + '/' + data.record.ContractFile + '"><i class="fa fa-file" style="font-size:16px;">&nbsp;</i></a>' +
                                    '<i class="fa fa-2x fa-trash-o hide pointer removeClick" ' +
                                    'data-href="/Admin/UsersManagement/RemoveUserContract" ' +
                                    'data-Id="' + data.record.UserId + '"></i></div>';
                            }
                        }
                        return '----';
                    }
                },
                UserDetails: {
                    title: 'جزئیات',
                    sorting: false,
                    display: function (data) {
                        return '<div class="userDetails pointer" data-UserId="' + data.record.UserId + '"><i class="fa fa-list-alt"></i></div>';
                    }
                },
                IsActivated: {
                    title: 'فعال&nbsp;/&nbsp;غیرفعال',
                    sorting: false,
                    input: function (data) {
                        if (data.record != undefined) { //if (data.record != '') {
                            if (data.record.IsActivated == true) {
                                return '<select id="Edit-IsActivated" name="IsActivated">' +
                                    '<option value="true" selected>بله</option>' +
                                    '<option value="false">خیر</option>' +
                                    '</select>';
                            }
                            else {
                                return '<select id="Edit-IsActivated" name="IsActivated">' +
                                    '<option value="true">بله</option>' +
                                    '<option value="false" selected>خیر</option>' +
                                    '</select>';
                            }
                        }
                        else {
                            return '<select id="Edit-IsActivated" name="IsActivated">' +
                                '<option value="true">بله</option>' +
                                '<option value="false">خیر</option>' +
                                '</select>';
                        }
                    },
                    options: {
                        true: 'بله',
                        false: 'خیر'
                    },
                    display: function (data) {
                        ////debugger;
                        if (data.record.IsActivated)
                            return '<div class="isActivated" data-UserId="' + data.record.UserId + '"><i class="fa fa-check-square"></i></div>';
                        else
                            return '<div class="isActivated" data-UserId="' + data.record.UserId + '"><i class="fa fa-square-o"></i></div>';
                    }
                },
                IsDeleted: {
                    title: 'حذف&nbsp;شده',
                    sorting: false,
                    input: function (data) {
                        if (data.record != undefined) { //if (data.record != '') {
                            if (data.record.IsDeleted == true) {
                                return '<select id="Edit-IsDeleted" name="IsDeleted">' +
                                    '<option value="true" selected>بله</option>' +
                                    '<option value="false">خیر</option>' +
                                    '</select>';
                            }
                            else {
                                return '<select id="Edit-IsDeleted" name="IsDeleted">' +
                                    '<option value="true">بله</option>' +
                                    '<option value="false" selected>خیر</option>' +
                                    '</select>';
                            }
                        }
                        else {
                            return '<select id="Edit-IsDeleted" name="IsDeleted">' +
                                '<option value="true">بله</option>' +
                                '<option value="false">خیر</option>' +
                                '</select>';
                        }
                    },
                    options: {
                        true: 'بله',
                        false: 'خیر'
                    },
                    display: function (data) {
                        ////debugger;
                        if (data.record.IsDeleted)
                            return '<div class="isDeleted" data-UserId="' + data.record.UserId + '"><i class="fa fa-check-square"></i></div>';
                        else
                            return '<div class="isDeleted" data-UserId="' + data.record.UserId + '"><i class="fa fa-square-o"></i></div>';
                    }
                },
                EditUser: {
                    title: 'ویرایش',
                    sorting: false,
                    display: function (data) {
                        return '<div class="editUser pointer" data-UserId="' + data.record.UserId + '"><i class="fa fa-edit"></i></div>';
                    }
                }
            },
            formCreated: function (event, data) {

            },
            formSubmitting: function (event, data) {

            },
            formClosed: function (event, data) {

            },
            selectionChanged: function () {
                debugger;
                var $selectedRows = $('#listContainer').jtable('selectedRows');

                if ($selectedRows.length > 0) {
                    selectedRows.SelectedRowsForDelete = [];
                    $selectedRows.each(function () {
                        debugger;
                        var record = $(this).data('record');
                        selectedRows.SelectedRowsForDelete.push(record.UserId);
                    });
                    selectedRows.ParamName = 'UserIds';
                    if ($('.jtable-column-header-selecting .jtable-column-header-container input').is(':checked')) {
                        $('.jtable-column-header-selecting .jtable-column-header-container').css('background-position', '-22px 0');
                    }
                    else {
                        $('.jtable-column-header-selecting .jtable-column-header-container').css('background-position', 'unset');

                    }

                } else {

                    $('.jtable-column-header-selecting .jtable-column-header-container').css('background-position', 'unset');
                }
            },
            recordAdded: function (event, data) {

            },
            recordUpdated: function (event, data) {

            },
            recordDeleted: function (event, data) {

            },
            recordsLoaded(event, data) {
                //debugger;

                var rows = $('.jtable-data-row');
                for (var i = 0; i < rows.length; i++) {
                    var row = $('.jtable-data-row')[i];
                    var isActivated = $(row).find('td:nth-child(2) div.isActivated').html();
                    var isDeleted = $(row).find('td:nth-child(2) div.isDeleted').html();
                    if (isActivated == 'true' && isDeleted == 'false') {
                        //$(row).css('background-color', '#5cb85c');//green
                    }
                    else
                        if (isActivated == 'true' && isDeleted == 'true') {
                            $(row).css('background-color', '#cc9999');//red
                            //$(row).css('opacity', '0.5');//opacity=0.5
                        }
                        else
                            if (isActivated == 'false' && isDeleted == 'true') {
                                $(row).css('background-color', '#ff3333');//red
                            }
                            else
                                if (isActivated == 'false' && isDeleted == 'false') {
                                    $(row).css('background-color', '#f0ad4e');//yellow
                                }
                }

                debugger;
                var isResponsiveList = $.cookie("IsResponsiveList");

                if (isResponsiveList == "") {

                    if ($('.jtable').data('basictable') != null)
                        $('.jtable').basictable('destroy');

                    //$('.jtable').basictable({
                    //    breakpoint: 2000
                    //});

                    $('.jtable').basictable({
                        //containerBreakpoint: 1024
                        breakpoint: 1024
                    });

                    //$('div.jtable-main-container').css('display', 'inherit');

                }
                else
                    if (isResponsiveList == 'True') {
                        if ($('.jtable').data('basictable') != null)
                            $('.jtable').basictable('destroy');

                        $('.jtable').basictable({
                            //breakpoint: 2000
                            breakpoint: 2000
                        });

                        $('div.jtable-main-container').css('width', '100%');
                        $('div.jtable-main-container').css('display', 'inline-block');

                        //$('.jtable').basictable({
                        //    containerBreakpoint: 800
                        //});
                    }
                    else
                        if (isResponsiveList == 'False') {
                            $('.jtable').css('overflow-x', 'scroll');
                            $('.jtable').css('width', 'inherit');
                            $('div.jtable-main-container').css('display', 'inline-block');
                        }

                var $selectedRows = $('#listContainer').jtable('selectedRows');
                if ($selectedRows.length > 0) {
                    $selectedRows.click();
                }
            }
        });

        $('.jtable-page-number-next').css('color', 'red')

        //$('#listContainer').jtable('load');
        //if ($('.jtable').data('basictable') != null)
        //    $('.jtable').basictable('destroy');

        //$('.jtable').basictable({
        //    containerBreakpoint: 800
        //});
        $('.jtable-page-number-next').html("بعدی")


        $('#btnSearch').click(function (e) {
            e.preventDefault();
            debugger;
           
            var slcDomainAndRole = $('#slcDomainAndRole').val(); //دامنه و نقش
            var slcDomainSearch = $('#slcDomainSearch').val();
            var slcRoleIdsSearch = $('#slcRoleIdsSearch').val(); 
            var slcLevelIdsSearch = $('#slcLevelIdsSearch').val();//دسترسی
            var slcParentIdSearch = $('#slcParentIdSearch').val(); //والد
            var txtUserNameSearch = $('#txtUserNameSearch').val(); // نام کاربری
            var txtRegisterDateTimeFromSearch = $('#txtRegisterDateTimeFromSearch').val(); // تاریخ ثبت از
            var txtRegisterDateTimeToSearch = $('#txtRegisterDateTimeToSearch').val(); //تاریخ ثبت تا 
            var txtNameSearch = $('#txtNameSearch').val(); //نام
            var txtFamilySearch = $('#txtFamilySearch').val(); //نام خانوادگی
            var txtEmailSearch = $('#txtEmailSearch').val(); //ایمیل
            var txtPhoneSearch = $('#txtPhoneSearch').val(); //تلفن
            var txtMobileSearch = $('#txtMobileSearch').val(); //همراه
            var txtAddressSearch = $('#txtAddressSearch').val(); //آدرس
            var txtCertificateIdSearch = $('#txtCertificateIdSearch').val(); //شماره شناسنامه
            var txtNationalCodeSearch = $('#txtNationalCodeSearch').val(); //کد ملی
            var txtBirthDateTimeFromSearch = $('#txtBirthDateTimeFromSearch').val(); //تاریخ تولد از
            var txtBirthDateTimeToSearch = $('#txtBirthDateTimeToSearch').val(); //تاریخ تولد تا

            if (slcDomainAndRole != '' ||
                slcDomainSearch != '' ||
                slcRoleIdsSearch != '' ||
                slcLevelIdsSearch != '' ||
                slcParentIdSearch != '' ||
                txtUserNameSearch != '' ||
                txtRegisterDateTimeFromSearch != '' ||
                txtRegisterDateTimeToSearch != '' ||
                txtNameSearch != '' ||
                txtFamilySearch != '' ||
                txtEmailSearch != '' ||
                txtPhoneSearch != '' ||
                txtMobileSearch != '' ||
                txtAddressSearch != '' ||
                txtCertificateIdSearch != '' ||
                txtNationalCodeSearch != '' ||
                txtBirthDateTimeFromSearch != '' ||
                txtBirthDateTimeToSearch != '') {
                $('#listContainer').jtable('load', {
                    currentPage: $('#listContainer').jtable('getCurrentPage'),
                    ParentId: slcParentIdSearch,
                    DomainSettingId: slcDomainSearch,
                    DomainAndRole: slcDomainAndRole,
                    RoleIds: slcRoleIdsSearch,
                    LevelIds: slcLevelIdsSearch,
                    UserName: txtUserNameSearch,
                    Name: txtNameSearch,
                    Family: txtFamilySearch,
                    Email: txtEmailSearch,
                    Phone: txtPhoneSearch,
                    Mobile: txtMobileSearch,
                    Address: txtAddressSearch,
                    CertificateId: txtCertificateIdSearch,
                    NationalCode: txtNationalCodeSearch,
                    BirthDateTimeFrom: txtBirthDateTimeFromSearch,
                    BirthDateTimeTo: txtBirthDateTimeToSearch,
                    RegisterDateTimeFrom: txtRegisterDateTimeFromSearch,
                    RegisterDateTimeTo: txtRegisterDateTimeToSearch,
                    IsActivated: null
                });
            }
            else {
                $('#my-warning-modal').find('.modal-title').html('راهنمایی');
                $('#my-warning-modal').find('#error').html('مقادیر را وارد کنید');
                $('#my-warning-modal').modal("show");
            }
        });

        $('#btnCreateExcel').click(function (e) {
            e.preventDefault();
            debugger;

            
            var slcDomainAndRole = $('#slcDomainAndRole').val();
            var slcDomainSearch = $('#slcDomainSearch').val();
            var slcRoleIdsSearch = $('#slcRoleIdsSearch').val();
            var slcLevelIdsSearch = $('#slcLevelIdsSearch').val();
            var slcParentIdSearch = $('#slcParentIdSearch').val();
            var txtUserNameSearch = $('#txtUserNameSearch').val();
            var txtRegisterDateTimeFromSearch = $('#txtRegisterDateTimeFromSearch').val();
            var txtRegisterDateTimeToSearch = $('#txtRegisterDateTimeToSearch').val();
            var txtNameSearch = $('#txtNameSearch').val();
            var txtFamilySearch = $('#txtFamilySearch').val();
            var txtEmailSearch = $('#txtEmailSearch').val();
            var txtPhoneSearch = $('#txtPhoneSearch').val();
            var txtMobileSearch = $('#txtMobileSearch').val();
            var txtAddressSearch = $('#txtAddressSearch').val();
            var txtCertificateIdSearch = $('#txtCertificateIdSearch').val();
            var txtNationalCodeSearch = $('#txtNationalCodeSearch').val();
            var txtBirthDateTimeFromSearch = $('#txtBirthDateTimeFromSearch').val();
            var txtBirthDateTimeToSearch = $('#txtBirthDateTimeToSearch').val();

            //if (slcDomainSearch != '' ||
            //    slcRoleIdsSearch != '' ||
            //    slcLevelIdSearch != '' ||
            //    slcParentIdSearch != '' ||
            //    txtUserNameSearch != '' ||
            //    txtRegisterDateTimeFromSearch != '' ||
            //    txtRegisterDateTimeToSearch != '' ||
            //    txtNameSearch != '' ||
            //    txtFamilySearch != '' ||
            //    txtEmailSearch != '' ||
            //    txtPhoneSearch != '' ||
            //    txtMobileSearch != '' ||
            //    txtAddressSearch != '' ||
            //    txtCertificateIdSearch != '' ||
            //    txtNationalCodeSearch != '' ||
            //    txtBirthDateTimeFromSearch != '' ||
            //    txtBirthDateTimeToSearch != '') {
            $.ajax({
                cache: false,
                type: "Post",
                url: "/Admin/UsersManagement/CreateExcel",
                data: {
                    ParentId: slcParentIdSearch,
                    DomainSettingId: slcDomainSearch,
                    DomainAndRole: slcDomainAndRole,
                    RoleIds: slcRoleIdsSearch,
                    LevelIds: slcLevelIdsSearch,
                    UserName: txtUserNameSearch,
                    Name: txtNameSearch,
                    Family: txtFamilySearch,
                    Email: txtEmailSearch,
                    Phone: txtPhoneSearch,
                    Mobile: txtMobileSearch,
                    Address: txtAddressSearch,
                    CertificateId: txtCertificateIdSearch,
                    NationalCode: txtNationalCodeSearch,
                    BirthDateTimeFrom: txtBirthDateTimeFromSearch,
                    BirthDateTimeTo: txtBirthDateTimeToSearch,
                    RegisterDateTimeFrom: txtRegisterDateTimeFromSearch,
                    RegisterDateTimeTo: txtRegisterDateTimeToSearch,
                    IsActivated: null
                },
                success: function (result) {
                    debugger;
                    if (result.Result == "OK") {
                        if (result.FileName != '') {
                            var url = '/Admin/UsersManagement/Download?fileName=' + result.FileName;
                            window.open(url, '_blank');
                        }
                    }
                    else
                        if (result.Result == "ERROR") {
                            $('#my-warning-modal').find('.modal-title').html('خطا');
                            $('#my-warning-modal').find('#error').html(result.Message);
                            $('#my-warning-modal').modal("show");
                        }
                },
                error: function (xhr, ajaxOptions, thrownError) {
                    $('#my-warning-modal').find('.modal-title').html('خطا');
                    $('#my-warning-modal').find('#error').html('خطا');
                    $('#my-warning-modal').modal("show");
                }
            });
            //}
            //else {
            //    $('#my-warning-modal').find('.modal-title').html('راهنمایی');
            //    $('#my-warning-modal').find('#error').html('مقادیر را وارد کنید');
            //    $('#my-warning-modal').modal("show");
            //}
        });

        $('#btnReload').click(function () {
            $('#btnReset').click();
            $('#slcRoleIdsSearch').val('').trigger("chosen:updated");
            $('#slcLevelIdsSearch').val('').trigger("chosen:updated");
            $('#listContainer').jtable('load');
        });

        $('body').on('click', '.isActivated', function () {
            ////debugger;
            var _this = $(this);
            var userId = _this.attr('data-UserId');
            $.ajax({
                cache: false,
                type: "Post",
                url: "/Admin/UsersManagement/ToggleActivationUser",
                //data: JSON.stringify({ 'LevelId': levelId }),
                data: { 'UserId': userId },
                //contentType: 'application/json',
                //dataType: "json",
                success: function (result) {
                    if (result.Result == "OK") {

                        var slcDomainSearch = $('#slcDomainSearch').val();
                        var slcRoleIdsSearch = $('#slcRoleIdsSearch').val();
                        var slcLevelIdsSearch = $('#slcLevelIdsSearch').val();
                        var slcParentIdSearch = $('#slcParentIdSearch').val();
                        var txtUserNameSearch = $('#txtUserNameSearch').val();
                        var txtRegisterDateTimeFromSearch = $('#txtRegisterDateTimeFromSearch').val();
                        var txtRegisterDateTimeToSearch = $('#txtRegisterDateTimeToSearch').val();
                        var txtNameSearch = $('#txtNameSearch').val();
                        var txtFamilySearch = $('#txtFamilySearch').val();
                        var txtEmailSearch = $('#txtEmailSearch').val();
                        var txtPhoneSearch = $('#txtPhoneSearch').val();
                        var txtMobileSearch = $('#txtMobileSearch').val();
                        var txtAddressSearch = $('#txtAddressSearch').val();
                        var txtCertificateIdSearch = $('#txtCertificateIdSearch').val();
                        var txtNationalCodeSearch = $('#txtNationalCodeSearch').val();
                        var txtBirthDateTimeFromSearch = $('#txtBirthDateTimeFromSearch').val();
                        var txtBirthDateTimeToSearch = $('#txtBirthDateTimeToSearch').val();

                        if (slcDomainSearch != '' ||
                            slcRoleIdsSearch != '' ||
                            slcLevelIdsSearch != '' ||
                            slcParentIdSearch != '' ||
                            txtUserNameSearch != '' ||
                            txtRegisterDateTimeFromSearch != '' ||
                            txtRegisterDateTimeToSearch != '' ||
                            txtNameSearch != '' ||
                            txtFamilySearch != '' ||
                            txtEmailSearch != '' ||
                            txtPhoneSearch != '' ||
                            txtMobileSearch != '' ||
                            txtAddressSearch != '' ||
                            txtCertificateIdSearch != '' ||
                            txtNationalCodeSearch != '' ||
                            txtBirthDateTimeFromSearch != '' ||
                            txtBirthDateTimeToSearch != '') {
                            $('#listContainer').jtable('load', {
                                currentPage: $('#listContainer').jtable('getCurrentPage'),
                                ParentId: slcParentIdSearch,
                                DomainSettingId: slcDomainSearch,
                                RoleIds: slcRoleIdsSearch,
                                LevelIds: slcLevelIdsSearch,
                                UserName: txtUserNameSearch,
                                Name: txtNameSearch,
                                Family: txtFamilySearch,
                                Email: txtEmailSearch,
                                Phone: txtPhoneSearch,
                                Mobile: txtMobileSearch,
                                Address: txtAddressSearch,
                                CertificateId: txtCertificateIdSearch,
                                NationalCode: txtNationalCodeSearch,
                                BirthDateTimeFrom: txtBirthDateTimeFromSearch,
                                BirthDateTimeTo: txtBirthDateTimeToSearch,
                                RegisterDateTimeFrom: txtRegisterDateTimeFromSearch,
                                RegisterDateTimeTo: txtRegisterDateTimeToSearch,
                                IsActivated: null
                            });
                        }
                        else {
                            $('#listContainer').jtable('load');
                        }
                    }
                    else
                        if (result.Result == "ERROR") {
                            $('#my-warning-modal').find('.modal-title').html('خطا');
                            $('#my-warning-modal').find('#error').html('خطا');
                            $('#my-warning-modal').modal("show");
                        }
                    //else
                    //    if (result.Result == "NotAccess") {
                    //        $('#my-warning-modal').find('.modal-title').html('خطا');
                    //        $('#my-warning-modal').find('#error').html(NotAccess);
                    //        $('#my-warning-modal').modal("show");
                    //    }
                },
                error: function (xhr, ajaxOptions, thrownError) {
                    debugger;
                    //if (xhr.status == 403) {
                    //    showErrorMessage(ErrorTitle, xhr.Result);
                    //}
                    //else {
                    //    showErrorMessage(ErrorTitle, ErrorMessage);
                    //}
                    //$('#my-warning-modal').find('.modal-title').html('خطا');
                    //$('#my-warning-modal').find('#error').html('خطا');
                    //$('#my-warning-modal').modal("show");
                }
            });
        });

        $('body').on('click', '.isDeleted', function () {
            var _this = $(this);
            var userId = _this.attr('data-UserId');
            $.ajax({
                cache: false,
                type: "Post",
                url: "/Admin/UsersManagement/ToggleTemporaryDeleteUser",
                //data: JSON.stringify({ 'LevelId': levelId }),
                data: { 'UserId': userId },
                //contentType: 'application/json',
                //dataType: "json",
                success: function (result) {
                    if (result.Result == "OK") {

                        var slcDomainSearch = $('#slcDomainSearch').val();
                        var slcRoleIdsSearch = $('#slcRoleIdsSearch').val();
                        var slcLevelIdsSearch = $('#slcLevelIdsSearch').val();
                        var slcParentIdSearch = $('#slcParentIdSearch').val();
                        var txtUserNameSearch = $('#txtUserNameSearch').val();
                        var txtRegisterDateTimeFromSearch = $('#txtRegisterDateTimeFromSearch').val();
                        var txtRegisterDateTimeToSearch = $('#txtRegisterDateTimeToSearch').val();
                        var txtNameSearch = $('#txtNameSearch').val();
                        var txtFamilySearch = $('#txtFamilySearch').val();
                        var txtEmailSearch = $('#txtEmailSearch').val();
                        var txtPhoneSearch = $('#txtPhoneSearch').val();
                        var txtMobileSearch = $('#txtMobileSearch').val();
                        var txtAddressSearch = $('#txtAddressSearch').val();
                        var txtCertificateIdSearch = $('#txtCertificateIdSearch').val();
                        var txtNationalCodeSearch = $('#txtNationalCodeSearch').val();
                        var txtBirthDateTimeFromSearch = $('#txtBirthDateTimeFromSearch').val();
                        var txtBirthDateTimeToSearch = $('#txtBirthDateTimeToSearch').val();

                        if (slcDomainSearch != '' ||
                            slcRoleIdsSearch != '' ||
                            slcLevelIdsSearch != '' ||
                            slcParentIdSearch != '' ||
                            txtUserNameSearch != '' ||
                            txtRegisterDateTimeFromSearch != '' ||
                            txtRegisterDateTimeToSearch != '' ||
                            txtNameSearch != '' ||
                            txtFamilySearch != '' ||
                            txtEmailSearch != '' ||
                            txtPhoneSearch != '' ||
                            txtMobileSearch != '' ||
                            txtAddressSearch != '' ||
                            txtCertificateIdSearch != '' ||
                            txtNationalCodeSearch != '' ||
                            txtBirthDateTimeFromSearch != '' ||
                            txtBirthDateTimeToSearch != '') {
                            $('#listContainer').jtable('load', {
                                currentPage: $('#listContainer').jtable('getCurrentPage'),
                                ParentId: slcParentIdSearch,
                                DomainSettingId: slcDomainSearch,
                                RoleIds: slcRoleIdsSearch,
                                LevelIds: slcLevelIdsSearch,
                                UserName: txtUserNameSearch,
                                Name: txtNameSearch,
                                Family: txtFamilySearch,
                                Email: txtEmailSearch,
                                Phone: txtPhoneSearch,
                                Mobile: txtMobileSearch,
                                Address: txtAddressSearch,
                                CertificateId: txtCertificateIdSearch,
                                NationalCode: txtNationalCodeSearch,
                                BirthDateTimeFrom: txtBirthDateTimeFromSearch,
                                BirthDateTimeTo: txtBirthDateTimeToSearch,
                                RegisterDateTimeFrom: txtRegisterDateTimeFromSearch,
                                RegisterDateTimeTo: txtRegisterDateTimeToSearch,
                                IsActivated: null
                            });
                        }
                        else {
                            $('#listContainer').jtable('load');
                        }
                    }
                    else
                        if (result.Result == "ERROR") {
                            $('#my-warning-modal').find('.modal-title').html('خطا');
                            $('#my-warning-modal').find('#error').html('خطا');
                            $('#my-warning-modal').modal("show");
                        }
                },
                //error: function (xhr, ajaxOptions, thrownError) {
                //    $('#my-warning-modal').find('.modal-title').html('خطا');
                //    $('#my-warning-modal').find('#error').html('خطا');
                //    $('#my-warning-modal').modal("show");
                //}
            });
        });

        $('body').on('click', '.editUser', function (e) {
            e.preventDefault();
            var _this = $(this);
            var userId = _this.attr('data-UserId');
            window.location.href = '/Admin/UsersManagement/EditUser/' + userId;
        })

        $('.jtable-toolbar-item:nth-child(2) .jtable-toolbar-item-text').prepend('<div style="display: inline-block;vertical-align: middle;line-height: 0;"><i class="fa fa-warning"></i>&nbsp;&nbsp;</div>');

        var isResponsiveList = $.cookie("IsResponsiveList");

        if (isResponsiveList == 'True')
            $('.jtable-toolbar-item:nth-child(3) .jtable-toolbar-item-text').prepend('<div style="display: inline-block;vertical-align: middle;line-height: 0;"><i class="fa fa-toggle-on"></i>&nbsp;&nbsp;</div>');
        else
            $('.jtable-toolbar-item:nth-child(3) .jtable-toolbar-item-text').prepend('<div style="display: inline-block;vertical-align: middle;line-height: 0;"><i class="fa fa-toggle-off"></i>&nbsp;&nbsp;</div>');

        $('body').on('click', '.hierarchyOfUsers', function (e) {
            e.preventDefault();
            var _this = $(this);
            var userId = _this.attr('data-UserId');
            window.location.href = '/Admin/UsersManagement/HierarchyOfUsers/' + userId;
        });

        $('body').on('click', '.HierarchyOfTreeViewUsers', function (e) {
            e.preventDefault();
            var _this = $(this);
            var userId = _this.attr('data-UserId');
            window.location.href = '/Admin/UsersManagement/HierarchyOfTreeViewUsers/' + userId;
        });

        $('body').on('click', '.userDetails', function (e) {
            e.preventDefault();
            var _this = $(this);
            var userId = _this.attr('data-UserId');
            window.location.href = '/Admin/UsersManagement/GetUserDetails/' + userId;
        });

        //var backgroundInterval = setInterval(function () {
        //    $div2blink.toggleClass("backgroundRed");
        //}, 1500)

        //$('div.jtable-main-container div.jtable-title div.jtable-toolbar span.jtable-toolbar-item')
        //  .animate({ color: 'red' }, 400)
        //  .delay(400)
        //  .animate({ color: 'black' }, 3000);

        //$('div.jtable-main-container div.jtable-title div.jtable-toolbar span.jtable-toolbar-item')
        //  .animate({ Color: 'red' }, 400, 'linear')
        //  .delay(400)
        //  .animate({ Color: original_color }, 2800, 'easeOutCirc');

        //$('.jtable').basictable({
        //    forceResponsive: false,
        //    /*breakpoint: 1600,*/
        //});

        //$('#table-container-breakpoint').basictable({
        //    containerBreakpoint: 485
        //});

        $('#searchBlock').hide();


        $('body').on('change', '#slcDomainAndRole', function (e) {
            e.preventDefault();
            debugger;

            var item = parseInt($(this).val());
            //همه

            if (item == 1) {

                roleId = 0;
                levelId = 0;


                $('#slcDomainSearch').val('');
                $('#slcRoleIdsSearch').val('');

                $('#slcDomainSearch').change();
                $('#slcDomainSearch').trigger("chosen:updated");


            } else if (item == 2) { // ملکاوان
                debugger;

                roleId = 3;//users
                levelId = 20; // آگهی دهنده


                $('#slcDomainSearch').val(3); //melkavan.com
                $('#slcRoleIdsSearch').val(3);//users

                $('#slcDomainSearch').change();
                $('#slcDomainSearch').trigger("chosen:updated");



            } else if (item == 3) { //تنیاکو

                roleId = 2; //admins
                levelId = 0;

                $('#slcDomainSearch').val(1); //my.teniaco.com
                $('#slcRoleIdsSearch').val(2);

                $('#slcDomainSearch').change();
                $('#slcDomainSearch').trigger("chosen:updated");


            } else if (item == 4) { //سرمایه گذاران

                roleId = 3; //users
                levelId = 5; //سرمایه گذاران

                $('#slcDomainSearch').val(1);//my.teniaco.com
                $('#slcRoleIdsSearch').val(3); //users

                $('#slcDomainSearch').change();
                $('#slcDomainSearch').trigger("chosen:updated");



            } else if (item == 5) { //مشاورین املاک
                roleId = 2; //admins
                levelId = 22; //مشاورین املاک

                $('#slcDomainSearch').val(1); //my.teniaco.com
                $('#slcRoleIdsSearch').val(2); //admins

                $('#slcDomainSearch').change();
                $('#slcDomainSearch').trigger("chosen:updated");

                $('#slcLevelIdsSearch').val(4050).trigger("chosen:updated");
            }
        });

        $('#slcDomainAndRole').val('3');
        $('#slcDomainAndRole').change();
        $('#btnSearch').click();
    });
</script>
