@using VM.Melkavan
@using VM.Public;
@using Newtonsoft.Json

@{
    ViewData["Title"] = "نمودار بازدید";
}

<script>
    @{
        List<AdvertisementViewersVM> viewers = new List<AdvertisementViewersVM>();
        viewers = (List<AdvertisementViewersVM>)ViewData["Viewers"];
        @Html.Raw("var advertisementViewers = [];")
        ;

        @Html.Raw("debugger; advertisementViewers = " +
             JsonConvert.DeserializeObject(JsonConvert.SerializeObject(viewers)) + "; debugger;")
        ;

    }
</script>


<script src="/lib/Highcharts-11.1.0/code/highcharts.js"></script>
<script src="/lib/Highcharts-11.1.0/code/highcharts-more.js"></script>
<style>


    #container {
        background: white;
    }

    .highcharts-credits {
        visibility: hidden;
    }



    .highcharts-figure,
    .highcharts-data-table table {
        min-width: 320px;
        max-width: 800px;
        margin: 1em auto;
    }

    .highcharts-data-table table {
        font-family: Verdana, sans-serif;
        border-collapse: collapse;
        border: 1px solid #ebebeb;
        margin: 10px auto;
        text-align: center;
        width: 100%;
        max-width: 500px;
    }

    .highcharts-data-table caption {
        padding: 1em 0;
        font-size: 1.2em;
        color: #555;
    }

    .highcharts-data-table th {
        font-weight: 600;
        padding: 0.5em;
    }

    .highcharts-data-table td,
    .highcharts-data-table th,
    .highcharts-data-table caption {
        padding: 0.5em;
    }

    .highcharts-data-table thead tr,
    .highcharts-data-table tr:nth-child(even) {
        background: #f8f8f8;
    }

    .highcharts-data-table tr:hover {
        background: #f1f7ff;
    }

    #chart {
        background-color: white;
        border: none;
        color: black;
        font-size: 20px;
        cursor: pointer;
        width: 80px;
        height: 40px;
        margin-right: 12px;
        margin-top: 5px;
    }

    #container {
        min-height: 0vh !important;
    }
</style>